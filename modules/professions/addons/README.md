# Professions Addons

WoW addons for profession and crafting capabilities belong here.

Each addon uses its own technical directory. The current addon is:

```text
modules/professions/addons/ProfessionTracker
```

`ProfessionTracker` keeps its historical technical name to preserve
existing SavedVariables compatibility.
