export type GuildMemberInput = {
  name: string;
  realm: string;
  region: string;
  className: string;
  level: number;
  rank: string;
  rankIndex: number;
  note: string | null;
  officerNote: string | null;
};
